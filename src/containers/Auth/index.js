import React, { PropTypes } from 'react';
import styles from './index.css';

const Auth = () => (
    <div className={styles.wrapper}>
        Auth
    </div>
);

Auth.propTypes = {

};

export default Auth;
